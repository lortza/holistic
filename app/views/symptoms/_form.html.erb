<%= form_for(@symptom) do |f| %>
  <% if @symptom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@symptom.errors.count, "error") %> prohibited this symptom from being saved:</h2>

      <ul>
      <% @symptom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :system_id, "System Name" %><br>
    <%= f.collection_select :system_id, System.all, :id, :name, :prompt => "-- Select One --" %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
